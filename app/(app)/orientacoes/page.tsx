import Orientacoes from "@/components/Orientacoes";

export const dynamic = "force-dynamic";

// Orientações Gerenciais (observação por conta, com histórico), dentro do shell.
export default function Page() {
  return <Orientacoes />;
}
