import Carteira from "@/components/Carteira";

export const dynamic = "force-dynamic";

// Carteira de Contas (lista as contas por gestor; edita o responsável), dentro do shell.
export default function Page() {
  return <Carteira />;
}
