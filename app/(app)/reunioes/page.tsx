import Reunioes from "@/components/Reunioes";

export const dynamic = "force-dynamic";

// Pautas e Reuniões (agenda da agência), dentro do shell de navegação.
export default function Page() {
  return <Reunioes />;
}
